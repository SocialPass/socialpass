{
  "name": "@socialpass/frontend",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start": "yarn workspace @socialpass/sdk start",
    "build": "yarn workspace @socialpass/sdk build",
    "test": "yarn workspace @socialpass/sdk test",
    "lint": "yarn workspace @socialpass/sdk lint",
    "prepare": "yarn workspace @socialpass/sdk prepare",
    "size": "yarn workspace @socialpass/sdk size",
    "analyze": "yarn workspace @socialpass/sdk analyze",
    "storybook": "yarn workspace @socialpass/sdk storybook",
    "build-storybook": "yarn workspace @socialpass/sdk build-storybook"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "tsdx": "0.14.1"
  }
}
