{
  "name": "@nfty/frontend",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "postinstall": "lerna bootstrap",

    "build:sdk": "yarn workspace @nfty/sdk build",
    "start:sdk": "",

    "build:whitelabel": "yarn build:sdk && yarn workspace @nfty/whitelabel build",
    "start:whitelabel": "lerna run --scope '{@nfty/sdk,@nfty/whitelabel}' dev --parallel"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
