{
  "name": "@socialpass/frontend",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start": "yarn workspace @socialpass/tokengate start",
    "build": "yarn workspace @socialpass/tokengate build",
    "test": "yarn workspace @socialpass/tokengate test",
    "lint": "yarn workspace @socialpass/tokengate lint",
    "prepare": "yarn workspace @socialpass/tokengate prepare",
    "size": "yarn workspace @socialpass/tokengate size",
    "analyze": "yarn workspace @socialpass/tokengate analyze",
    "storybook": "yarn workspace @socialpass/tokengate storybook",
    "build-storybook": "yarn workspace @socialpass/tokengate build-storybook"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "tsdx": "0.14.1"
  }
}
