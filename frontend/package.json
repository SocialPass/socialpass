{
  "name": "@nfty/frontend",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start": "yarn workspace @nfty/sdk start",
    "build": "yarn workspace @nfty/sdk build",
    "test": "yarn workspace @nfty/sdk test",
    "lint": "yarn workspace @nfty/sdk lint",
    "prepare": "yarn workspace @nfty/sdk prepare",
    "size": "yarn workspace @nfty/sdk size",
    "analyze": "yarn workspace @nfty/sdk analyze",
    "storybook": "yarn workspace @nfty/sdk storybook",
    "build-storybook": "yarn workspace @nfty/sdk build-storybook"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "tsdx": "0.14.1"
  }
}
