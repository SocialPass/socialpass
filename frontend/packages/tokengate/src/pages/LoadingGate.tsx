import React from 'react';
import { TokenGateContext } from '../context';
import { Loading } from '../components';

// TicketGate Component
export const LoadingGate = (): JSX.Element => {
	return (
		<Loading/>
	)
}
