# Ensure we have up-to-date build tools
setuptools>=68.0.0
wheel>=0.41.0

# Use binary packages when available but don't restrict to only binaries
--prefer-binary
-r config/requirements/production.txt

# Netlify specific requirements
whitenoise==6.6.0  # For serving static files
dj-database-url==2.1.0  # For parsing DATABASE_URL
psycopg2-binary==2.9.9  # Binary version of psycopg2 for Netlify
python-dotenv==1.0.0  # For loading environment variables 